spring:
  datasource:
    # Isolated in-memory DB for tests — the main profile's file H2 must not
    # accumulate rows from @SpringBootTest runs (dev data pollution).
    url: jdbc:h2:mem:majo-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
